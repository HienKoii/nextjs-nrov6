import Error from "@/components/error";

export default function TransactionHistoryPage() {
  return (
    <div>
      <Error text="Server free đéo cần nạp nên đéo làm trang này nhé !" />
    </div>
  );
}
